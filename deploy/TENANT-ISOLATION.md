# GenUI Tenant Isolation Statement

**Audience**: the security team of a customer running a GenUI deployment.

**Scope**: one GenUI deployment (one backend, one Redis, one Qdrant) serving multiple tenants, the customer's access architecture (e.g. insurance: `agente` vs `assicurato`; enterprise: `dipendente` vs `cliente`).

**Claim**: no request authenticated as tenant A can read, write, delete or influence data belonging to tenant B.

**As of**: 2026-07-20. References name the file and the symbol, never a line number: a line number is wrong as soon as anything above it moves, and a reader who follows a stale one lands on unrelated code. Re-verify any row with `grep -n "<symbol>" <file>`. `backend/tests/test_deploy_docs.py` fails when a named file or symbol disappears, and when a line number reappears.

## The one rule everything derives from

**The tenant of a request is resolved server-side from the presented API key, never from anything the client controls.** Keys are mapped to tenants in server config (`CLIENT_API_KEYS` / `ADMIN_API_KEYS`, entries `key:tenant`). No request body, header or query field can name a tenant.

- Key → tenant resolution: `KeyRegistry.authenticate` (`backend/auth/keys.py`)
- Enforced on every route as a FastAPI dependency: `require_client` / `require_admin` (`backend/auth/dependencies.py`)
- **Fail-closed**: with no keys configured, privileged routes refuse (403). The only override is the explicit dev flag `GENUI_DEV_OPEN=1` (`open_mode_context`, `backend/auth/identity.py`), which must never be set in a customer deployment; `deploy/smoke.sh` fails if it is.

Every store below then namespaces its data by that resolved tenant. Isolation is enforced at the storage key / query filter, not by route-level discipline: a new route reusing these stores inherits the boundary.

## Boundary by data type

| Data                                           | Store                      | Boundary mechanism                                                                                                                                                                                                                                                                                                    | Enforced at                                                                                                                                                                                                                                                                                                                                        | Test                                                                              |
| ---------------------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Knowledge base (RAG documents)                 | Qdrant                     | Every chunk is stamped `tenant` at index time; every search/list/delete/stats applies a mandatory tenant filter. Named tenants use strict payload equality; the default tenant additionally matches legacy points with no tenant field (single-tenant upgrades keep working).                                         | stamp: `index_chunks` (`backend/rag/vector_store.py`); filter builder: `_tenant_condition` (`rag/vector_store.py`); applied in `search_async`, `delete_by_source`, `list_documents` and `get_collection_stats` (`rag/vector_store.py`); routes pass the key's tenant: `upload_document`, `search_documents`, `list_documents`, `delete_document`, `get_document_stats` (`backend/api/main.py`) | `tests/test_kb_tenant_filter.py`                                                  |
| RAG inside chat `/query`                       | n/a                        | The retrieval tool the model can invoke is a per-request closure that captures the authenticated tenant; the model cannot pass a tenant.                                                                                                                                                                              | `_run_search_tool`, the per-request closure built inside `process_query_async` (`backend/agents/response_agent.py`); route: `process_query` (`backend/api/main.py`)                                                                                                                                                                                                                                                               | `tests/test_query_isolation.py`                                                   |
| User profiles                                  | Redis (in-memory fallback) | Storage key `genui:profile:{tenant}:{user_id}`. Cross-tenant reads are impossible by key construction; within a tenant, reads/writes/deletes/exports of a `user_id` additionally require a user token signed with that tenant's HMAC secret (or an admin key). A request without a usable identity stores nothing: the store refuses to key a profile on a blank or placeholder id.                                                                | key: `ProfileStore._key` (`backend/profiles/store.py`), identity check: `is_identified` (`backend/profiles/store.py`); per-user authz: `authorize_user_access` (`backend/auth/identity.py`), shared guard `check_user_access` (`backend/auth/dependencies.py`); routes: `sync_profile`, `get_profile`, `export_user_data`, `delete_profile` (`backend/api/main.py`)                                                                                       | `tests/test_identity.py`, `tests/test_profiles.py`, `tests/test_dsar.py`                                |
| User identity tokens                           | n/a                        | HMAC-SHA256 secrets are configured per tenant (`USER_TOKEN_SECRETS="secret:tenant"`); verification requires the token's tenant claim to equal the key's tenant, so a token minted for tenant A never authorizes anything under tenant B. A configured secret is always enforced; `GENUI_DEV_OPEN` does not bypass it. | `UserTokenVerifier.verify` (`backend/auth/identity.py`)                                                                                                                                                                                                                                                                                    | `tests/test_identity.py`                                                          |
| Zone render cache                              | Redis (in-memory fallback) | Cache key = `{tenant}:{zone_id}:{config_hash}:{segment}`: a tenant can neither read nor poison another tenant's cached renders.                                                                                                                                                                                       | `_cache_key_for` (`backend/api/zone_router.py`); `build_cache_key` (`backend/utils/zone_cache.py`)                                                                                                                                                                                                                                   | `tests/test_zone_registry.py` (tenant isolation case), `tests/test_zone_cache.py` |
| Cached render _content_                        | n/a                        | Any render that is cached and shared is generated from the **segment archetype** (a pure function of the cache key), never from the requesting user's raw profile: the first requester of a segment cannot inject content served to others (cache poisoning closed).                                                  | `_agent_request` (`backend/api/zone_router.py`); `segment_archetype` (`backend/segmentation/segmenter.py`)                                                                                                                                                                                                                            | `tests/test_segment_archetype.py`                                                 |
| Governed zone config (registry)                | Redis (in-memory fallback) | Storage key `genui:zonecfg:{tenant}:{zone_id}`; resolution reads only the authenticated tenant's entry.                                                                                                                                                                                                               | `ZoneConfigStore._key` (`backend/zones/registry.py`); `_apply_registry` (`backend/api/zone_router.py`)                                                                                                                                                                                                                               | `tests/test_zone_registry.py`                                                     |
| Experiment metrics (impressions/clicks/uplift) | Redis (in-memory fallback) | Counter key `{tenant}:{zone_id}:{arm}`; ingestion and stats use the key's tenant.                                                                                                                                                                                                                                     | `MetricsStore._key` (`backend/metrics/store.py`); `ingest_events` and `event_stats` (`backend/api/events_router.py`)                                                                                                                                                                                                                                                      | `tests/test_observability.py`                                                     |
| Audit log ("what was shown to whom")           | logger sink / file         | Every record carries the tenant resolved from the key; a per-tenant extract is a filter on that field.                                                                                                                                                                                                                | `AuditLogger.log` (`backend/utils/audit.py`); callers pass `auth.tenant` throughout `api/main.py`, `api/zone_router.py`, `api/events_router.py`                                                                                                                                                                                              | `tests/test_rate_limit_audit.py`                                                  |
| LLM budget (BYOK spend)                        | Redis (in-memory fallback) | `LLM_BUDGET_PER_HOUR` is counted per tenant: one tenant's traffic cannot exhaust another tenant's generations.                                                                                                                                                                                                        | budget limiter identity = tenant: `_budget_tenant`, `_charge_llm_budget` (`backend/api/zone_router.py`)                                                                                                                                                                                                                                                                    | `tests/test_cost_controls.py`                                                     |
| Rate limits                                    | Redis (in-memory fallback) | Counted per API key fingerprint; keys belong to one tenant.                                                                                                                                                                                                                                                           | `require_client` (`backend/auth/dependencies.py`); `RateLimiter.allow` (`backend/utils/rate_limit.py`)                                                                                                                                                                                                                                                                        | `tests/test_rate_limit_audit.py`, `tests/test_cost_controls.py`                   |
| Operational metrics (`/metrics`)               | Redis hash                 | Labeled by tenant; the endpoint itself is operator surface and requires an **admin** key.                                                                                                                                                                                                                             | `OpsMetrics.observe_generation` (`backend/metrics/ops.py`); route: `metrics` (`backend/api/main.py`)                                                                                                                                                                                                                                                                               | `tests/test_observability.py`                                                     |

## What tenants DO share (read this before signing)

Honesty about the boundary's nature: inside one deployment, isolation is **logical, not physical**.

- **The process.** All tenants are served by the same Python workers. There is no per-tenant memory partition; isolation relies on the key namespacing and filters above.
- **Redis and Qdrant instances.** Separation is by key prefix (Redis) and mandatory payload filter (Qdrant), not separate databases. Nothing except the backend can reach them (the deploy compose publishes neither).
- **The LLM engine and its key (BYOK, per-deployment).** Tenants share the engine; the per-tenant budget separates spend, and prompts for shared renders are built from segment archetypes, not raw user data.
- **The operator.** Whoever administers the VM sees everything. This document makes no claim against the deployment's own operator.

If two audiences must not share even these (separate failure domain, separate engine key, physical separation): run **two deployments**. The model is one-command-per-deployment precisely so that this remains cheap (`deploy/README.md` §Bring-up).

## Failure posture

- **No keys configured** → privileged routes fail closed (403), they do not degrade to open. (`backend/auth/identity.py`)
- **Redis unavailable** → stores fall back to bounded per-process memory and reconnect with backoff. The fallback uses the same tenant-namespaced keys, so isolation properties are preserved; what degrades is durability and cross-worker consistency, visibly (`GET /health` → `"redis": "reconnecting"`, status `degraded`). (`backend/utils/redis_conn.py`,
  `tests/test_redis_reconnect.py`)
- **Embedding/LLM misconfiguration** → loud startup or request errors, never a silent fallback to a different provider (a typo must not send documents to a third party). (`backend/llm/embeddings.py`, `tests/test_embeddings.py`)

## How to re-verify this document

```bash
# Full suite (includes every test cited above):
cd backend && python3 -m unittest discover -s tests

# Live, against a running deployment (fail-closed + per-tenant scoping):
cd deploy && ./smoke.sh

# Spot-check any reference after code changes, e.g.:
grep -n "_tenant_condition" backend/rag/vector_store.py
```
