# GenUI Output Guarantees Statement

**Audience**: the legal, compliance or security team of an entity running a GenUI deployment. This document is written to be attached to a contract.

**Scope**: every piece of LLM-generated UI served by one GenUI deployment: zone renders (`/zone/render`, `/zone/render/stream`, `/zone/batch-render`, warmup, background refreshes) and chat responses (`/query`).

**Claim**: the properties below are enforced by code that runs **after** generation, on **every** serving path. They hold regardless of what the model outputs, which model is plugged in, or how it is prompted.

**As of**: 2026-07-20. References name the file and the symbol, never a line number: a line number is wrong as soon as anything above it moves, and a reader who follows a stale one lands on unrelated code. Re-verify any row with `grep -n "<symbol>" <file>`. `backend/tests/test_deploy_docs.py` fails when a named file or symbol disappears, and when a line number reappears.

## The one principle everything derives from

**A guarantee is a check on the output, never an instruction to the model.**
Prompt instructions shift probability; they bound nothing, and their violations are invisible. Every guarantee in the table below is a post-generation gate with a test that goes red if the gate is removed: the recorded model responses in `backend/tests/golden/` are deliberately adversarial (invented links, invented prices, missing pinned items), so the suite fails if any link of the chain stops working.

The chain, in order, on all three paths (zone sync, zone SSE, chat):

```
schema validation → URL whitelist → numeric grounding → content policy → redundancy → pinned enforcement
```

## Enforced guarantees

| #   | Guarantee                                                                                                                                                                                                                                                                                                                                                                                                | Mechanism                                                                                      | Enforced at                                                                                                                                                     | Test                                                                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1   | **No broken schema.** Every component served matches its published schema; a malformed component is dropped individually and reported, it never reaches the page or breaks the render.                                                                                                                                                                                                                   | Per-component Pydantic validation (built-ins) / JSON Schema validation (host-registered types) | `validate_components`: `backend/schemas/components.py`; called on all paths: `backend/agents/zone_agent.py`, `backend/agents/response_agent.py` | `tests/test_component_schemas.py`, `tests/test_golden_zone.py`                            |
| 2   | **No invented link.** A URL survives only if it existed verbatim in the input (pinned content, developer prompts, RAG documents, page context). Dangerous schemes (`javascript:`, `data:`, ...) are always removed, whitelist on or off. Chat prose gets the same treatment: markdown links to non-input URLs collapse to their text, and model-claimed sources pass the same rules.                     | Whitelist built from the request input, checked against the output                             | `UrlGuard`: `backend/utils/url_guard.py`; chat text: `backend/agents/response_agent.py`                                                              | `tests/test_url_guard.py`, `tests/test_output_guarantees.py`, `tests/test_golden_zone.py` |
| 3   | **No invented number where a number is the content.** A `stats_banner` value, a `pricing_cards` price, a `chart` data point survives only if its digits trace to a number present in the input (verbatim modulo formatting: `1,200`, `1200` and `1200.0` match each other). An ungrounded stat or plan is removed; one ungrounded chart point drops the whole chart (a series missing one bar misleads). | Grounding set built from the request input, checked against the displayed values               | `NumericGuard`: `backend/utils/numeric_guard.py`; wired: `backend/agents/zone_agent.py`, `backend/agents/response_agent.py`                  | `tests/test_output_guarantees.py`, `tests/test_golden_zone.py`                            |
| 4   | **No banned term (per tenant).** Banned terms never reach the page: a component containing one is dropped whole; chat text is redacted. Matching is case-insensitive, word-boundary, phrase-aware. Terms come from two merged sources: the `CONTENT_POLICY` env (the deployment-wide seed, including the global `"*"`) and a per-tenant store an admin edits from the Studio with no redeploy (`effective_policy` = env + stored). Misconfigured env policy fails loudly (renders degrade to pinned-only fallback), it never silently disables.                                                                      | Post-generation scan of every string in the component data                                     | `ContentPolicy`: `backend/utils/content_policy.py`; sources merged in `effective_policy`: `backend/utils/content_policy_store.py`; write path `backend/api/content_policy_router.py`; wired: `backend/agents/zone_agent.py`, `backend/agents/response_agent.py`              | `tests/test_output_guarantees.py`, `tests/test_content_policy_governance.py`                                                         |
| 4b  | **No zone that says the same thing twice.** The components of a zone are read as one band, but the model writes them in one shot, so it repeats: two CTAs to the same URL, then a card echoing the primary CTA's label and link. The same target twice inside one component loses the repeat; an element with the same target AND the same wording as an earlier component is removed; a component emptied that way is dropped whole. Pinned content is exempt (it runs after). | Link targets and wording compared across the components of one render | `RedundancyGuard`: `backend/utils/redundancy_guard.py`; wired: `backend/agents/zone_agent.py` (sync chain and inside the stream loop) | `tests/test_redundancy_guard.py`, `tests/test_output_guarantees.py` (sync + SSE cases) |
| 5   | **Pinned content always appears, exactly once.** Presence is verified on the actual output (by URL or title), not on the model's claims; missing items are appended. Presence is read from the WHOLE component tree, so a pinned link the model used as a hero CTA or a plan button counts as shown instead of being appended a second time. Pinned items are operator-authored input, so they are exempt from guards 3-4b by construction (they run last).                                                                                                    | Post-generation verification and append                                                        | `_enforce_pinned` + `_collect_shown`: `backend/agents/zone_agent.py`                                                                                                           | `tests/test_golden_zone.py` (missing-pinned fixture), `tests/test_output_guarantees.py` (hero-CTA case)                                      |
| 6   | **Every removal is visible.** What the chain removed is reported in the response: `meta.sanitization.{dropped_components, removed_urls, removed_numbers, policy_violations}` on zone renders and `/query` alike, and every served render is audit-logged (what was shown, to whom, from which cache state).                                                                                              | Sanitization report assembled where the payload is built                                       | `_payload_from_result` (`backend/api/zone_router.py`); `process_query_async` (`backend/agents/response_agent.py`); `to_frontend_response` (`backend/agents/orchestrator.py`)                                               | `tests/test_output_guarantees.py` (meta case)                                             |
| 7   | **Streaming is not a bypass.** The SSE path applies the identical chain per component before it crosses the wire; the terminal `complete` event is the authoritative, fully enforced result.                                                                                                                                                                                                             | Same guard instances applied inside the stream loop                                            | `render_zone_stream_async`, calling the same `_validate_and_sanitize` (`backend/agents/zone_agent.py`)                                                                                                                          | `tests/test_output_guarantees.py` (SSE case)                                              |
| 8   | **Cached content was enforced at generation time.** The chain runs before `cache.set`; a cached render is a previously enforced render. Shared (cached) renders are additionally generated from the segment archetype, never from the requesting user's raw profile (see `deploy/TENANT-ISOLATION.md`).                                                                                                  | Chain inside the agent, caching outside                                                        | `_render_cold`: `backend/api/zone_router.py`                                                                                                            | `tests/test_segment_archetype.py`                                                         |

Escape hatches (operator-controlled, on by default): `URL_WHITELIST_ENABLED`, `NUMERIC_GROUNDING_ENABLED`, `DEDUP_COMPONENTS_ENABLED`. Disabling one is a deployment decision that weakens the corresponding row; dangerous URL schemes stay blocked regardless.

## Honest limits: what is best-effort, not guaranteed

A guarantee that overpromises is worse than none. The following are real limits of the mechanisms above:

- **Numeric grounding is verbatim, not semantic.** It guarantees the digits shown existed in the input; it does not verify the _claim around_ the number. "120 countries" with 120 in the input passes even if the input said "120 employees". Magnitude conversion is not attempted: a model that rewrites "10,000,000" as "10M" loses that stat (removed, reported). The model is instructed to copy figures as-is; the instruction is the quality lever, the guard is the guarantee.
- **Numbers in prose are not checked.** Grounding applies to fields whose value IS the number (stat values, prices, chart points). Digits inside descriptions, labels or markdown text are not enforced: stripping them would mangle legitimate prose. If a number must be guaranteed, put it in a `stats_banner` / `pricing_cards` / `chart`, or pin it.
- **Tone is not enforceable.** `CONTENT_POLICY` enforces term absence, which a regex can prove. Tone constraints ("formal", "no superlatives") remain prompt-level instructions: real influence, no guarantee. Nothing in this document claims otherwise.
- **Banned-term matching is lexical.** Word-boundary and case-insensitive, but not semantic: misspellings, synonyms or paraphrases of a banned term are not caught. The blocklist guarantee is "this exact term never appears", not "this topic never appears".
- **Redundancy is judged on targets and wording, not on meaning.** The guard proves "this link with this label was already shown". A component repeating an earlier link under genuinely different wording survives, because whether it adds anything is an editorial call: dropping it would silently thin out legitimate output. Component-to-component coherence beyond that (does the second block deserve its place?) is prompt-level, best effort.
- **Text content of allowed components is model-authored.** Outside the enforced fields above, wording is the model's (that is the product). The measurement loop (`/events/stats`) and the audit trail exist to observe it, and pinned content exists to fix what must be exact.

## How to re-verify this document

```bash
cd backend && python3 -m unittest discover -s tests           # full suite
python3 -m unittest tests.test_output_guarantees -v            # this document's chain
python3 -m unittest tests.test_golden_zone -v                  # adversarial end-to-end
```

The golden harness replays deliberately hostile model output through the real pipeline. Neutering any guard in the chain turns it red.
